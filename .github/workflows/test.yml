name: CI

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - "**/*.md"
      - ".github/ISSUE_TEMPLATE/**"

  push:
    branches:
      - main
    tags:
      - "v*"
    paths-ignore:
      - "**/*.md"
      - ".github/ISSUE_TEMPLATE/**"

  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: "20.19.0"

jobs:
  cli:
    if: github.repository == 'taroify/taroify'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install
        run: yarn install --frozen-lockfile --ignore-scripts

      - name: Check catalog
        run: yarn run check:catalog

      - name: Test CLI and MCP
        run: yarn run test:cli

  lint:
    if: github.repository == 'taroify/taroify'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install
        run: yarn install --frozen-lockfile

      - name: Run linter
        run: yarn run lint

  test:
    if: github.repository == 'taroify/taroify'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn run build:packages

      - name: Run test cases
        run: yarn run test:coverage --watchman=false

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: ./coverage

  site:
    if: github.repository == 'taroify/taroify'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install
        run: |
          yarn install --frozen-lockfile --ignore-scripts
          yarn --cwd site install --frozen-lockfile --ignore-scripts

      - name: Build documentation site
        run: yarn --cwd site site:build
        env:
          CHANGELOG_STRICT: "false"
